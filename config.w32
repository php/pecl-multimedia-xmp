// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("xmp", "for xmp support", "no");

// Otherwise, use ARG_ENABLE
ARG_ENABLE("xmp", "enable xmp support", "no");

if (PHP_XMP != "no") {
	EXTENSION("xmp", "php_xmp.c");
}

